# Demo workspace memory

This file is loaded once per conversation by **MemoryManager** when long-term memory is enabled.

- The user is exploring the **agent-sdk** web demo.
- Prefer concise answers and mention when a capability comes from the SDK (tools, MCP, skills, streaming, sessions).
