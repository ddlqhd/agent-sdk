/**
 * User-visible text when a tool is skipped or fails because the run was aborted
 * (same {@link StreamOptions.signal} as model streaming).
 */
export const TOOL_USER_ABORTED_MESSAGE = 'The operation was aborted.';
